# Alaya v0.2.0 Closeout Report

Status: CANDIDATE. Local deterministic gates pass; release acceptance is
blocked on credentialed live pi-mono smoke evidence.
Date: 2026-05-11.
Controller branch: `work/v0.2.0-complete` from `main` at `27e0867`.

This report records the local v0.2.0 candidate state: pi-mono Garden
provider wiring, recall scoring refinements, Trustworthy Memory Loop
trace anchoring, and invariant §25 SemVer coverage for MCP/protocol
public surfaces.

## Scope

- Integrated `fe89e28` — pi-mono Garden provider path, removal of the
  unused engine-gateway `ConversationProvider` placeholder, and lazy
  daemon Garden compute provider resolver.
- Integrated `feb2da0` — recall pressure-ratio penalty, per-call
  tokenizer hints, per-domain activation-weight overrides, and
  resolved scoring factors.
- Integrated `1f4563e` — `source_delivery_ids` on agent-originated
  signal/proposal paths, proposal persistence, migration `066`, and
  daemon trace-audit coverage.
- Bumped workspace package metadata to v0.2.0 so the advertised
  `@do-soul/alaya-protocol` `^0.2.0` consumer pin matches the package
  surface.
- Added v0.2.0 SemVer closeout docs and strengthened the
  `packages/protocol/src/__tests__/semver-surface.test.ts` snapshot
  tripwire with schema signature hashes for optionality, nullability,
  enum/literal values, defaults, and basic checks.
- Kept v0.2.1 and v0.2.2 follow-ups out of scope:
  `#BL-009` remains deferred to v0.2.1; `#BL-037` and `#BL-038` remain
  deferred to v0.2.2.

## Review/Fix Loop

Round 1 found Blocking / Important issues. Local fixes landed for:

- v0.2.0 package metadata drift (`0.1.3` -> `0.2.0`).
- Runtime Garden compute PATCH now refreshes routing candidates without
  daemon restart.
- `source_delivery_ids` are validated against recorded deliveries in the
  current trusted context before agent-originated signal/proposal writes.
- The SemVer snapshot now hashes schema signatures, not only object keys.
- Runtime/release docs no longer claim credentialed pi-mono live smoke
  proof in this environment.

Round 2 re-review is required after this report update. The remaining
known release blocker is the Slice 3 credentialed live pi-mono smoke.

## Verification

Integrated controller verification:

- `rtk pnpm install --frozen-lockfile` passed and synchronized the local
  workspace install after the pi-ai dependency landed.
- `rtk pnpm exec tsc -b` passed.
- `rtk pnpm build` passed.
- `rtk pnpm test` passed after review fixes: 292 test files, 2346
  tests.
- `rtk git diff --check` passed.

Lane verification was run before each lane commit:

- Provider lane: `rtk pnpm build`, targeted engine-gateway, soul, and
  core-daemon Vitest suites, provider-reference greps, and
  `rtk git diff --check` passed.
- Recall lane: `rtk pnpm build`, protocol/core/core-daemon Vitest
  suites, stale `/ 4` estimator greps, and `rtk git diff --check`
  passed.
- Trust lane: `rtk pnpm build`, protocol/storage/core/core-daemon
  Vitest suites, trace-anchor greps, and `rtk git diff --check` passed.

Docs and SemVer checks:

- `rtk rg -n "MCP and Protocol SemVer Contract" docs/handbook/invariants.md`
  found invariant §25.
- `rtk rg -n "Deprecated public symbols" docs/handbook/maintenance.md`
  found the deprecation-tracking section.
- `rtk rg -n "BL-008|BL-009|BL-037|BL-038" docs/handbook/backlog.md`
  found the v0.2.0 resolution and v0.2.1/v0.2.2 deferrals.
- `rtk rg -n "ConversationProvider|resolveLanguageModel" docs/handbook/runtime-status.md`
  returned no active runtime-status hits.
- `rtk rg -n "ConversationProvider|resolveLanguageModel" packages apps docs/handbook/runtime-status.md`
  returned no active code/runtime-status hits.
- `packages/protocol/src/__tests__/semver-surface.test.ts` and its
  snapshot exist under `packages/protocol/src/__tests__/`.
- `wc -c packages/protocol/src/__tests__/__snapshots__/semver-surface.test.ts.snap`
  reported 29804 bytes, keeping the generated snapshot under the
  repository 30 KB full-read discipline.
- `rtk node -e ...` secret-presence check showed
  `ALAYA_OFFICIAL_GARDEN_SECRET_REF`, `ALAYA_GARDEN_OPENAI_SECRET_REF`,
  `ALAYA_OPENAI_SECRET_REF`, `OPENAI_API_KEY`, `PI_API_KEY`, and
  `ALAYA_TEST_OPENAI_KEY` absent, without printing secret values.

Review-fix verification added after Round 1:

- `rtk pnpm exec vitest run --project @do-soul/alaya-protocol -u -- semver-surface`
  passed: 68 test files, 571 tests, 1 snapshot updated.
- `rtk pnpm exec vitest run --project @do-soul/alaya-soul -- compute-routing-service`
  passed: 24 test files, 199 tests.
- `rtk pnpm exec vitest run --project @do-soul/alaya-core-daemon -- apps/core-daemon/src/__tests__/routes-config-port.test.ts apps/core-daemon/src/__tests__/tool-runtime-bootstrap.test.ts apps/core-daemon/src/__tests__/mcp-memory-tool-handler.test.ts apps/core-daemon/src/__tests__/trustworthy-loop-trace.test.ts`
  passed: 67 test files, 459 tests.

## External Provider Smoke

No credentialed live pi-mono smoke was run in this environment. The
local release claim is limited to deterministic extractor, parser,
resolver, hot-config routing, build, and test proof. A release operator
with a configured official-provider secret must separately smoke the
live provider path through Garden before v0.2.0 can be marked PASS.

## Acceptance Mapping

- Slices 1-4: provider placeholder deletion, pi-mono extractor, official
  Garden provider swap, lazy runtime resolver, and hot config routing
  are implemented and covered by unit/integration tests. Slice 3's
  credentialed live provider transcript remains blocked by missing
  local secret configuration.
- Slices 5-7: recall pressure penalty, tokenizer hints, and per-domain
  weights are implemented with protocol defaults and scoring-factor
  audit output.
- Slices 8-9: Trustworthy Loop `source_delivery_ids` anchoring is
  validated against recorded deliveries and carried through MCP
  requests, EventLog payloads, proposal persistence, and the daemon
  trace proof.
- Slice 10: invariant §25, maintenance deprecation tracking, backlog
  closeout/deferrals, runtime/code-map updates, release notes, and the
  SemVer surface snapshot are present.
