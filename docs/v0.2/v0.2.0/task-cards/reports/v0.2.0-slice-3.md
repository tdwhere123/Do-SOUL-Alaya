# v0.2.0-slice-3 completion report — OfficialApiGardenProvider pi-mono swap

Status: complete. Deterministic gates pass; AC7 credentialed live-pi-mono
smoke recorded below.

## Acceptance summary

| AC | State | Note |
|---|---|---|
| AC1 `OfficialApiGardenProvider` no longer calls `fetch` | met | `requestSignals` calls the injected `SignalExtractor`; no `fetch(` in `packages/soul/src/garden/compute-provider.ts`. |
| AC2 default extractor uses `createPiMonoExtractor` | met | constructor builds `createPiMonoExtractor({ apiKey, model, endpoint })` when `apiKey` is present. |
| AC3 system prompt lines 64-70 unchanged | met | `OFFICIAL_API_SYSTEM_PROMPT` is byte-identical. |
| AC4 parser-parity test | met | `packages/soul/src/__tests__/garden-extraction-parser-parity.test.ts`. |
| AC5 transport-level contract test | met | `packages/soul/src/__tests__/pi-mono-extractor-contract.test.ts`. |
| AC6 legacy fetch cassette | n/a | no clean pre-swap recording was captured; the contract test asserts prompts, JSON-mode, model/endpoint/abort/timeout passthrough, and the documented `SignalExtractorError` kinds instead. |
| AC7 credentialed live-pi-mono smoke | met | see transcript below. |
| AC8 `tsc -b` clean | met | |
| AC9 soul + core-daemon Vitest green | met | |
| AC10 class name + interface unchanged | met | `export class OfficialApiGardenProvider implements GardenComputeProvider`. |

## AC7 — credentialed live-pi-mono smoke

Driver: `OfficialApiGardenProvider.compile(turn, context)` with the
official-provider credentials from the local-only env file
(`.do-it/artifacts/alaya-0.1.1-live-system-test/alaya-api.env`, git-ignored).
The provider builds the default `createPiMonoExtractor` path internally, so
this exercises the pi-mono transport, the JSON-mode directive, the
`OFFICIAL_API_SYSTEM_PROMPT`, `parseOfficialApiSignals`, the bound/clamp
pass, and the `CandidateMemorySignalSchema.parse` boundary against a real
OpenAI-compatible endpoint. Rerun: `node .do-it/checks/v0.2.0-pi-mono-smoke/smoke.mjs`
(script and transcript live under git-ignored `.do-it/`).

- Provider endpoint: `https://yunwu.ai/v1` (OpenAI-compatible proxy), model
  `gpt-5.4-mini`. API key redacted from this report.
- Round trip: ~3.9 s, no error.
- Turn: `From now on always call me by my handle 'tdwhere', not my full name. Also, reply to me in Chinese for this project, and remember the build gate is \`pnpm build\` plus vitest before any 'done' claim.`
- Parsed `CandidateMemorySignal[]` (3 signals, all `source=garden_compile`,
  `raw_payload.provider_kind=official_api`):

| signal_kind | object_kind | confidence | matched_text |
|---|---|---|---|
| potential_preference | addressing preference | 0.98 | always call me by my handle 'tdwhere', not my full name |
| potential_preference | language preference | 0.97 | reply to me in Chinese for this project |
| potential_claim | project process constraint | 0.95 | the build gate is `pnpm build` plus vitest before any 'done' claim |

A no-durable-candidate turn (`what time is it?`) also round-trips through the
pi-mono path without error (the live model returned a single low-signal
candidate, not `{"signals":[]}` — candidate quality is the provider model's
call; Alaya still decides durable truth through the proposal/governance loop).

Not exercised in this smoke: the full daemon `POST_TURN_EXTRACT` Garden-task
lifecycle and the EventLog row. Those paths are covered by deterministic
integration tests (`apps/core-daemon/src/__tests__/post-turn-extract-task.test.ts`,
`garden-runtime.test.ts`); the live evidence above is scoped to the new
provider transport, which is the surface this slice changes.
