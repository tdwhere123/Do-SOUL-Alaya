# v0.3.11 Implementation Checkpoint Report

Date: 2026-05-25
Status: **docs-truth checkpoint; release evidence pending**
Branch HEAD: `96e9bb9 feat(v0.3.11): calibrate weak-evidence abstention scores`

This report is not a final closeout report. It records current implementation
truth after commits `621fcec`, `8d2cbf7`, `8bf07c8`, and `96e9bb9`, plus the
separate staged cleanup of old full-diagnostics artifacts. Full public benches
on current HEAD are still pending.

## Scope Compliance

- Edited only v0.3.11 status/docs surfaces.
- The docs update worker did not edit `docs/bench-history/**` content and did
  not touch root `AGENTS.md`, root `CLAUDE.md`, source code, manifests, or
  lockfiles.
- The combined checkpoint staged set intentionally includes the artifact
  hygiene deletion of 23 old tracked full diagnostics under
  `docs/bench-history/**`.
- KPI, report, and pointer files under `docs/bench-history/**` are preserved;
  the staged deletion set is artifact hygiene, not release evidence.

## Evidence State

Current tracked bench pointers are legacy/stale baselines and do not prove
HEAD `96e9bb9`:

| Root | Current pointer state |
|---|---|
| `public/` | `latest-baseline*` only; old LongMemEval archives |
| `public-multiturn/` | `latest-baseline.json` only |
| `public-crossquestion/` | `latest-baseline.json` only |
| `public-locomo/` | `latest-baseline.json` only |

No current release claim may cite those pointers as v0.3.11 HEAD evidence.
Every new full bench must include `recall_token_economy`.

## Original Finding Map

| Finding | Current implementation evidence | Remaining verification state |
|---|---|---|
| B1 durable graph edges bypass governance | `621fcec` adds the edge proposal protocol/storage/core path, MCP/CLI review surfaces, and materialization-router tests. | Parent must re-review that automatic and bench-derived refs cannot create durable `memory_graph_edges` before governed acceptance. |
| B2 obsolete LoCoMo gates | `621fcec` updates release-gate code/tests for LoCoMo `0.55` embedding-off and `0.90` embedding-on targets. | Full LoCoMo off/on archives on current HEAD are still pending. |
| I1 graph endpoint workspace ownership | `621fcec` touches `graph-explore-service.ts` and core regressions. | Parent must confirm source and target endpoint workspace checks remain enforced through all edge creation paths. |
| I2 ambiguous `latest-baseline` semantics | `621fcec` documents `latest-run*`, `latest-passing*`, and legacy `latest-baseline*` semantics. | Current tracked roots have not yet produced HEAD `latest-run*` / `latest-passing*`; future full benches must exercise them. |
| I3 Phase 7 token-economy archive evidence missing | `621fcec` and prior Phase 7 work add recall-token-economy archive/report plumbing and tests. | No post-checkpoint full bench archive with `recall_token_economy` was found for HEAD `96e9bb9`. |
| I4 embedding provider calls undercounted | `621fcec` touches recall/token-economy accounting and related tests. | Full-bench cost numbers must not be published until current per-call accounting is verified on the release runs. |
| I5 strict-real live bench path red | `621fcec` touches the live runner and live-runner tests. | This checkpoint did not rerun the live test; parent should rerun the targeted bench-runner test before final closeout. |
| I6 huge generated diagnostics in release/source | `621fcec` moves docs toward compact diagnostics/external full artifacts; the artifact-hygiene worker staged 23 old full-diagnostics deletions. | Parent must keep the staged cleanup and verify packaging/source diff before release. |
| I7 local ONNX cache under install tree | `621fcec` touches `scripts/fetch-local-embedding-model.mjs` and README cache guidance. | The local ONNX model cache is currently missing; parent must supply/fetch it before embedding-on LoCoMo evidence. |
| I8 merged-shard token-economy report tests | Phase 7/report code exists in branch history; `621fcec` includes eval/history release-gate test work. | Parent should confirm direct render coverage for merged-shard token-economy omission/presence before final closeout. |
| I9 daemon embedding-policy offline transition tests | `621fcec` touches `daemon-embedding-runtime.test.ts`. | Parent should rerun focused daemon embedding-runtime tests and verify true-to-false provider availability pass-through. |
| N1 optimistic token-economy perf guard | `621fcec` touches recall-service token-economy tests. | Nice-to-have remains evidence-sensitive; do not use it as release blocker unless parent review escalates it. |

## Pending Release Gates

| Gate | Required result | Current checkpoint state |
|---|---|---|
| LongMemEval-S 500 off | R@5 >= 90% | pending full bench |
| LongMemEval-S multiturn 500 off | R@5 >= 90% | pending full bench |
| LongMemEval-S crossquestion 500 off | R@5 >= 90% | pending full bench |
| LoCoMo 1982 off | R@5 >= 55% | pending full bench |
| LoCoMo 1982 local ONNX on | R@5 >= 90% | pending model cache + full bench |
| Token economy | `recall_token_economy` in every new full bench | pending full bench evidence |

## Closeout Boundary

This checkpoint may support a docs-truth review. It must not be cited as final
release readiness, full evidence pass, or final B1/B2/I1-I9/N1 closure.
