> **Archived 2026-06-04** from the worktree-local `.do-it/plans/v0.3.11-completion-masterplan.md`
> (the `.do-it/` tree is gitignored and does not survive worktree removal). Plan-of-record for the
> v0.3.11 completion + the post-review closeout fix-loop, kept in tracked docs for durability.
> Internal `.do-it/findings/` and `.do-it/codex-review/` references below are historical
> (worktree-local); durable equivalents live in this `docs/v0.3/v0.3.11/` tree (see
> `reports/v0.3.11-closeout-report.md` and `reports/v0.3.11-independent-multilens-review-2026-06-04.md`).
> **No `v0.3.11` tag until the big-machine 500q R@5 gate passes.**

# v0.3.11 Completion — Master Plan (implementation-ready, codex-reviewed)

**Purpose:** authoritative, implementation-ready plan for closing v0.3.11 — recall quality
(LongMemEval-S R@5 → 90%) + completing the memory-system lifecycle (recall fan-in / compression /
forgetting) with **no hidden or half-built debt**. For operator + Codex, and the execution tracker.
Branch: `v0.3.11-completion` (worktree). Last updated: 2026-06-03.
**Incorporates** `.do-it/codex-review/v0.3.11-masterplan-review.md` (7 required changes folded in:
R0/R2/R3 rewritten as real contract work, B5/B6/B7 added, D-EDGEAUDIT promoted, phase-local gates,
R6 evidence contract).

**How to use (Codex):** plan-of-record. Evidence in `.do-it/findings/`; reviews in `.do-it/codex-review/`.
Orchestration: main thread orchestrates; implementer ≠ reviewer; **every phase ends with phase-local
gates (build/typecheck + targeted Vitest + doc/source-consistency sweep) AND a fresh review-loop
(reviewer + codex lens) to zero Blocking/Important before dependent phases proceed**; main thread edits
only `.do-it/*` + closeout docs.

---

## 0. OVERNIGHT RUN FINAL STATE (2026-06-04) — read first

R0–R4 implemented + per-phase review-looped (reviewer + red-team) to zero Blocking/Important. Two
critical issues were caught by review + fixed + re-reviewed ACCEPT: **B1** (a permanent-data-loss
TOCTOU in autonomous memory deletion — `compressed` capsule not re-verified at delete time; fixed
with a fail-closed delete-time re-verify) and **the fan-in vs I-1 contract conflict** (my R2 I-1
relevance-gate over-broadened and broke the R1/R2 earned-co_recalled fan-in; reconciled with a
gold-blind earned-edge-provenance discriminator — both contracts now hold).

**State: CLOSEOUT-COMPLETE (code) + GATE-PENDING — NOT merge-ready as an unqualified claim, NOT merged.**
The overnight run reached "green + MERGE-READY"; the codex independent multi-lens review (2026-06-04)
then found that claim premature (B1: docs said merge-ready while real code work + over-claimed LIVE/
default-OFF docs remained). The closeout fix-loop (see §0.2) has since CLOSED every codex finding
(B1–B5, I1–I6) and every morning decision, each wave implementer≠reviewer and review-looped to zero
Blocking/Important; `rtk pnpm build` exit 0 (full monorepo incl. inspector web). **Remaining before
merge-ready is honest:** (1) the big-machine **500q R@5 gate** — the real recall-quality validation,
still UNVALIDATED locally (WSL2 OOMs at 500q); (2) a final full-suite regression on a larger host (the
per-wave gates were targeted + the overnight pre-merge sweep, not a single post-fix-loop full run on
this box); (3) the operator merge itself. So "code-complete + reviewed", not "merge-ready".

**NOT auto-merged (deliberate):** `main` (`1e25a61` "ci: harden release and content gates") DIVERGED by
1 commit above the merge-base `b9f838f`; the branch is +160. So it is NOT a clean fast-forward — a
merge commit/rebase is needed, and `1e25a61` (content gates) may conflict with this branch's
README/CHANGELOG/docs. An unattended non-FF merge of the default branch + the pending morning decisions
made auto-merge the wrong call. **Operator merges in the morning** (resolve the `1e25a61` reconciliation,
then `git -C "<main repo>" merge v0.3.11-completion` / rebase). **No tag** — `v0.3.11` is tagged only
after the big-machine 500q gate passes.

**Morning decisions — SETTLED + IMPLEMENTED (2026-06-04, operator reviewed one-by-one).** Former
remaining CODE work **#1(A+B+C) + #2 + #5b** is done; #3/#4/#5a needed no code. All were finish-leg
work using existing machinery — no net-new subsystem.

- **#1 compress-arm `#BL-049` (task #64) — DONE A+B+C.** The closeout fix-loop activated the full
  autonomous forgetting lifecycle. `TOMBSTONE_GC` now runs the dormant→tombstone disposition sweep and
  physical GC; the delete authority is gated by disposition + 24h grace + delete-time re-verification.
  Compress is intentionally **lossy-summary**: the live capsule preserves shared evidence plus a
  deterministic gist summary, not each member's `content` byte-for-byte. Physical deletion is allowed
  only for members whose `evidence_refs` are a subset of the live capsule's evidence, and explicitly
  protected pinned / hazard / canon / consolidated memories are not compress-deleted.
- **#2 D-F1 reconciliation `#BL-050` — DONE default-ON + rule-only zero-cloud basis (§R4).** Ingest
  reconciliation now constructs with the rule-only port by default and can be disabled by operator env.
  The cloud/LLM judge remains optional and only affects the ambiguous UPDATE-vs-ADD band; exact
  duplicate NOOP runs locally with zero cloud.
- **#3 abstention `#BL-051` — CORRECTION: no production code.** `abstain` is empty in core/daemon —
  abstention is **bench-scoring only** (`apps/bench-runner/.../abstention.ts`, calibrated-confidence
  threshold `0.91` on `_abs` questions). Not a hidden debt. Threshold is 500q-data-dependent and must not
  be constant-tuned → validated at the 500q gate (#4).
- **#4 big-machine 500q gate — operator action, no code.** The real R@5→90% validation (UNVALIDATED
  locally; env recipe §R5): merge → pull on big box → decide paid extraction-fill (72277 turns,
  user-gated) → run. abstention(#3) validated here too.
- **#5a hop-2 earned fan-in — KEEP hop-1-only (defensible, no code).** The exemption flag is set only at
  direct path_expansion (recall-service.ts:2043, hop-1); a gold sibling reachable only via a 2-hop earned
  chain stays relevance-gated. **Fail-safe miss, never a mis-delivery**; hop-2 is transitive/weaker and the
  gate refusing it is correct. Revisit ONLY if the 500q gate shows 2-hop gold loss.
- **#5b enum invariant pin — DONE.** `co_recalled ∉ memoryGraphEdgeTypeValues` (it is a PathRelation
  relation_kind mapped to the display `recalls`) is pinned by the protocol invariant test.

**Commit log of the overnight run (newest first):** 539e32b (fan-in/I-1 reconcile) · fe328c3 (regression
gate: 2 stale-expectation fixes) · 63013f3 (G docs) · 9c738af (comment hygiene) · a2a2f52 (D-STUBS) ·
aa2e56c (#35 seed isolation) · dc08442 (R4-A B5/B7/D-EDGEAUDIT) · fb7d4fb (R3 closeout semver+janitor) ·
5227380 (R3 safety: B1+I3+gates) · d643be5 (R3d disposition gate) · 556abeb (R3e decay) · bdbabe0 (R3b/c
dormant+filter) · 450fa0a (R3a synthesis accept→create) · 8aa9f05/a2b52dd/9e2d3ad (R2 fan-in) ·
05f8012/5bc15fb/d7aaae1 (R1 co-recall) · 5ceae61/3decc57/eae265b/291e79b (R0 garden-compute) ·
36b4a5f/e0cf666/35915af (R0.1 B3).

---

## 0.2 Fix-loop implementation progress (live — 2026-06-04)

Closeout fix-loop after the codex independent multi-lens review
(`.do-it/codex-review/v0.3.11-completion-independent-multilens-review-2026-06-04.md`).
Serial waves (core typecheck closure), implementer ≠ reviewer, each wave review-looped to zero
Blocking/Important.

- **Wave 1a — forgetting data-safety hardening: DONE, reviewed zero.** B5 atomic compressed delete
  (`d1217b24`) + B2 audited dormant demotion (`61d585a1`); red-team/correctness found I-1 (dormant
  batch-abort) + I-2 (delete/audit cross-txn crash gap) → fixed atomically (`dcc970bb` I-2, `c9953080`
  I-1, append+mutation in one transaction); re-review CLOSED both.
- **Wave 1b — compress-arm ACTIVATION: DONE, reviewed zero.** #1A subset `source_memory_refs`
  (`5ab7f768`→tightened `e874f0a9`) + #1B protection-before-compressed (`fe49ad98`) + #1C enqueue
  TOMBSTONE_GC (`3155cf1d`) + stale-comment fix (`51a7c2a3`). Red-team found: compress is NOT
  fully low-loss — the capsule preserves the SHARED EVIDENCE (survives as evidence_capsules) + a
  deterministic gist summary, NOT the member's distilled `content`; membership tightened from
  evidence INTERSECTION to SUBSET so only FULLY-consolidated members compress (`e874f0a9`); honest
  comments. Plus FIX-3 no-silent-truncation diagnostic (`2436f5a8`), FIX-4 judged_useless delete-time
  re-verify (`7bdd1c58`), FIX-N1 not-protected backstop at tombstone authority (`c0fcc595`), FIX-N2
  strong-ref skip in disposition sweep (`5e40c492`), and disposition-sweep batch-abort skip-and-continue
  consistency (`7bb0483c`, discrimination in the daemon adapter — soul cannot import core). Re-review
  CLOSED all; autonomous forgetting (active→dormant→[compress|judged_useless]→tombstone→physical GC)
  is now LIVE, atomic, audited, fail-closed.
- **Completed follow-up waves:** Wave 2 = #2 reconciliation default-ON + rule-only zero-cloud basis
  (I3, `d57ace8a`, `90ba64a9`); Wave 3 = I2 EDGE_CLASSIFY requires verdict (`0eaa8bcc`); Wave 4 = I4
  production-path fan-in recall test + #5b co_recalled enum-invariant pin (`625c935d`, `53bc2b21`);
  Wave 5 = docs honesty for gate-pending status, runtime-status, backlog, README, and CHANGELOG
  (`9b050135`, `9111611c`, `06cc683f`, `d2c28fa9`).

---

## 1. North star + honest baseline
Goal: LongMemEval-S R@5 → 90% (real, category-balanced, embedding-OFF, production recall pipeline),
then close v0.3.11 with no fake deferrals. Operator stance: "deferred to v0.4/later" = incomplete work
in disguise → complete it; be honest finish-the-leg vs net-new; never tune a constant to bounce a number.

Baseline (500q OFF, `docs/bench-history/public/2026-06-02T145620Z-d73dcc2-policy-chat`): **R@1=52.0%
R@5=81.6% R@10=83.6%** (clean, llm_calls=0). 90%@50q was a small-sample artifact.

**Honest risk:** clean 90% on `under_ranked=57` NOT guaranteed (~88-89% floor on lme-s). LoCoMo-OFF
44.8% (target 55%); LoCoMo-ON unmeasured (target 90%); multiturn/crossquestion only stale 50q. The
500q recall-eval fast-loop OOMs on the local 7.6GB WSL2 box → full gate needs a larger host / single
heap-capped run.

---

## 2. The unified design insight
Recall (fan-in), compression (synthesis/consolidation), forgetting (decay/retire) are **ONE lifecycle**:
`decay → dormant → [compress | judge-useless] → retire/delete`. Terminal removal is gated by
compress-or-judge-useless, never crude decay→delete. Path-centric, silent, self-organizing,
self-forgetting plasticity ([[project_memory_system_design_philosophy]]). v0.3.11 completes it for
BOTH paths (mostly done) and MEMORIES (dead today).

---

## 3. Locked decisions (final)
- **B3 → DONE** (doc-align two-entry). 36b4a5f / e0cf666 / 35915af.
- **B4 → REFRAMED as a contract migration** (not a flag flip; see R0). Attached agent is the compute.
- **Recall fan-in → Route 乙**: durable ACCEPTED positive `memory_entry↔memory_entry` co-occurrence
  hub edges (member→representative) as the carrier; scorer stays memory_entry-only (comparable). Direct
  hub effects score mostly through `path_expansion`; `graph_expansion` only for leftover/multi-hop reach.
- **Forgetting-compression = one lifecycle**, completed for memories too (see R3). Negative/neutral
  terminal path retirement must ALSO pass the judge-useless gate or carry an explicit audit-provenance
  rationale (NOT a silent "default keep").
- **bench fidelity → B**: rule-based 6b co-occurrence/cross-link `recalls` edges in all harnesses.
- **no-deferral systemic**; net-new-large flagged honestly. **multi_hop_path → backlog `#BL-047`** (legit).

---

## 4. Complete problem set (after skeptical re-verification + codex completeness pass)

**REAL (in scope):**
| tag | item | phase | effort |
|---|---|---|---|
| L1/L2/L3 | recall multi-session fan-in non-load-bearing | R2 | — |
| B1 | 5 Tier-1 gates fail/stale/unmeasured | R5 | — |
| B2 | bench not production-fidelity for 6b | R1 | M |
| B4 | garden-compute residue + cloud-default + edge-LLM hardwired to cloud + host_worker not default | R0 | **L (contract migration)** |
| D-G1 | synthesis librarian-accept→create absent (memory compression entry) | R3 | M |
| D-F1 | ingest reconciliation default-OFF; reconcile compute routing | R0/R4 | M |
| D-FORGET | memory forgetting lifecycle dead (dormant/GC unwired+never-enqueued; no compress-before-delete gate; dormant rows still recall candidates) | R3 | **L** |
| D-DECAY-2 | time/idle decay; memory has last_used_at/last_hit_at NOT last_reinforced_at → field decision/migration | R3 | M |
| D-ABSTAIN | abstain_false_confident=9 misses — calibration (NOT threshold-bump). NUANCE: prior verdict (abstention-calibration-design.md) found grader-level calibration inert(OFF)/gaming(ON) → re-test against the 500q data, may route to a product evidence-strength signal | R2/R4 | M |
| D-SEED | seed-materialization batch-loss (candidate_absent=6 + 1963 dropped) — per-signal failure isolation + persisted reason (real bug) | R1/R3 | M |
| B5 | edge-proposal `expired`/`expires_at` enum+column exist, NO producer/sweeper → wire expiry OR delete dead enum/column | R4 | S–M |
| B6 | `alaya-eval longmemeval`/`self` print stub + `return 0` (false-green CI risk) → resolve (operator: complete-not-exclude) | R4 | S |
| B7 | auto-extractor doesn't populate `supersedes_refs`/`contradicts_refs` hints (MCP/test do) → bounded producer (operator: complete) | R4 | M |
| D-STUBS(b) | bare `alaya install` errors (documented verb); `--non-interactive json` works | R4 | S–M |
| D-EDGEAUDIT | **REQUIRED (not optional)**: re-point edge-failure audit to path-relation failure/health_inbox semantics (K4 needs auditable topology) | R4/R7 | S–M |
| McpBridge | standalone `tools.*` fallback "deferred to #BL-008" (daemon injects real handler) → delete or document | R4 | S |

**DROPPED / trivial (codex re-confirmed NOT real, or comment-only):** D-DECAY (competitive timer alive),
D-R7 (suppression implemented; stale comment → R5 hygiene), D-STUBS(c) CustomApiGardenProvider (delete
with R0 residue), D-LEASE (governance cache not on prod hot path → moot), "admin review UI" (Inspector
Governance page has proposals + health-inbox tabs, routes wired).

---

## 5. Implementation phases (ordered; core writers serial; each phase has its own gate + review-loop)

### R0 — Garden-compute CONTRACT MIGRATION (B4) — treat as L, not a flag flip
Acceptance (all required):
1. **Protocol + routing cleanup**: delete `local_model` + `custom_api` from `runtime-run.ts` provider
   kinds, `compute-routing.ts` + `compute-routing-service.ts` rankings, the throwing provider classes,
   and barrel exports; reconcile with `app-config.ts` (already narrowed to official_api/local_heuristics/
   host_worker).
2. **New host-worker B-2 edge-classification Garden task**: add a `GardenTaskKind` for edge classification
   (today only POST_TURN_EXTRACT + BULK_ENRICH exist; B-2 runs in-process in BULK_ENRICH via
   `edgeProducer.produceForNewMemory`). Defer the classify step out of synchronous enrichment into a
   claimable task; extend the MCP host_worker role + `complete_task` envelope (today only carries
   `candidate_signals`) to accept an edge-verdict result.
3. **Eventual-consistency contract (explicit)**: recall right after memory creation may run before
   host-worker edge classification completes → keep the deterministic heuristic as the immediate fallback;
   emit diagnostics for pending/stale edge-classify tasks. State this behavior in code + docs.
4. **host_worker as product default** for POST_TURN_EXTRACT + B-2, with explicit fallback for
   standalone/bench/no-agent (work must NOT sit silently pending), doctor/status warnings, and tests.
   `official_api` = explicit opt-in (bench/no-agent).
5. **Cloud edge-LLM default OFF** (`ALAYA_EDGE_PRODUCER_LLM_ENABLED` flips to default-off / strict opt-in;
   stop defaulting provider URL to yunwu) + a **no-network K4.5 regression** test.
6. D-F1 reconciliation per-fact decision routed through the same agent/rule path (not a self-cloud LLM).
- Files: `runtime-run.ts`, `app-config.ts`, `compute-routing*.ts`, `compute-provider.ts`, `garden-tier.ts`,
  `garden-runtime.ts`, `edge-auto-producer-*`, `edge-auto-producer-llm-adapter.ts`, `mcp-memory-tool-handler.ts`,
  `config-service.ts`, `install.ts`, `doctor`, `index.ts`, README/runtime-status. Task: #37 (rescoped L).
- Gate: build/typecheck + targeted Vitest + no-network K4.5 regression + reviewer + red-team(zero-cloud/gov) + codex lens.

### R1 — Bench fidelity B (the edges Route 乙 needs) — MUST precede R2
Acceptance: harnesses build rule-based 6b co-occurrence/cross-link → **minted, accepted, recall-eligible
positive `recalls` PathRelations** in the bench (archive currently has `recalls_edge_count=0`); NOT mere
pending proposals (B-1 cross-link proposals sit at conf 0.5 < 0.8 auto-accept → would leave the read side
unwired). Deterministic, LLM-free. Prove the archive now contains accepted recall-eligible positive paths.
- Files: `apps/bench-runner/src/**` + the minting path. Task: #34. Effort M. Gate: targeted Vitest + a smoke
  recall-eval confirming nonzero accepted positive paths.

### R2 — Recall fan-in (Route 乙) — the R@5 mechanism
Acceptance (hard):
- Carrier = ACCEPTED positive PathRelations minted from **real member `memory_entry` ids** (prerequisite:
  `source_memory_refs` may be empty / evidence_refs may be fresh-evidence — resolve member ids; this is a
  hard R1/R2 item). Hub shape member→representative.
- Direct hub effects score through `path_expansion` (graph skips path-admitted targets); `graph_expansion`
  only for leftover/multi-hop reach. Keep structural reserve + synthesis backstop + 5-class cohort
  diagnostics (I2). Fix `recall-cohort-fanin.test.ts` typecheck error.
- **Preserve the representative-selection guard**: nominate ONE query/evidence-relevant representative;
  REFUSE membership-only promotion (the temporary heuristic's guard, recall-service.ts:1501-1552) — else
  it promotes a non-gold rep → top-5 displacement.
- Scorer unchanged (memory_entry-only). Fast-loop proof (recall-eval --snapshot): R@1/5/10 + 4 miss buckets
  + **path_expansion vs graph_expansion gold-bearing top-5 movement (not plane counts)** + previously-hit→
  now-miss displacement (hard gate) + best-gold rank buckets + 5-class cohort attribution.
- Files: `recall-service.ts`, `recall-service-types.ts`, bench diagnostics. Task: #32. Serial core.
- Gate: build/typecheck + targeted Vitest + reviewer/red-team code review-loop. **R@5 efficacy
  (top-5 gold movement) is DEFERRED to the R5 big-machine 500q gate** — local 50q is operationally
  unreliable (OOM at 500q; the cached-extraction 50q needs the correct env and two prior local attempts
  ran on the wrong corpus). The R2 CODE (retire heuristic + durable carrier + reserve re-aim +
  path-vs-graph diagnostics) is reviewed here; whether the earned-edge fan-in actually moves R@5 is
  proven on the big machine. **Big-machine env recipe:** `set -a; . "/home/tdwhere/vibe/Do-SOUL Alaya/.do-it/bench-env/alaya-api.env"; set +a`
  (vars `ALAYA_OFFICIAL_GARDEN_API_KEY`/`ALAYA_OFFICIAL_GARDEN_SECRET_REF`, `OFFICIAL_API_GARDEN_MODEL=gpt-5.4-mini`);
  confirm `llm_calls=0`/`offline_fallbacks=0`/`path=official_api_compile` (cache-served, zero paid spend) before trusting.
  **OPEN RISK for morning:** if the fan-in is inert on the real corpus at R5, R1+R2 deliver the structural
  cleanup but NOT the R@5→90% headline — the earlier "fan-in inert" local finding was on the wrong
  (offline-fallback) corpus and is INVALID, so this is genuinely unknown until R5.

### R3 — Compression + forgetting lifecycle (G1 + D-FORGET + D-DECAY-2) — separate PATH vs MEMORY; sub-slices
The path lifecycle is mostly real; the MEMORY lifecycle is dead. Independent sub-slices (serial at merge):
- **R3a G1**: librarian/auditor synthesis review-proposal accept→create — add a `synthesis_create` branch
  to `prepareAcceptedProposalApply` (today only memory-update / path-governance) + deterministic no-LLM
  summary + atomic accept-with-events. (Memory compression ENTRY.) Task #40.
- **R3b memory dormant/GC wiring**: pass `dormantDemotionPort` + `tombstoneGcPort` into Janitor (today
  unwired) AND enqueue `DORMANT_DEMOTION` + `TOMBSTONE_GC` on a timer (today only TTL_CLEANUP enqueued).
- **R3c active-only recall filtering**: recall/list/FTS exclude DORMANT memory rows (today exclude only
  tombstoned; recall loads `findByWorkspaceId` directly → dormant memories stay candidates).
- **R3d memory delete-authority gate**: add a DURABLE memory compression/disposition marker
  ("compressed-into <capsule>" / "judged-useless") + enforce it in the hard-delete authority transaction
  (today hard delete checks only tombstoned + age → compress-before-delete NOT guaranteed). Extend the
  importance-gate (judge-useless, non-LLM) to memories. Negative/neutral terminal path retirement also
  gated here (or explicit audit-provenance rationale).
- **R3e timestamp/decay field decision**: memory entries have `last_used_at`/`last_hit_at`/counters, NOT
  `last_reinforced_at` (path-only) → decide the memory decay field (reuse last_used_at or add a field +
  migration); implement lazy time-decay (bounded, field-correct) at recall read; cold path retire-on-encounter.
- Files: `path-plasticity-service.ts`, `consolidation-*`, `synthesis-service.ts`, `materialization-router.ts`,
  `mcp-memory-proposal-workflow.ts`, `janitor.ts`, `garden-runtime.ts`, `dynamics-service.ts`,
  `memory-entry-repo.ts`, `memory-service.ts`, `memory-entry.ts`. Tasks #40/#46/#47. Serial core.
- Gate: build/typecheck + targeted Vitest + a "no tombstone before compression/disposition" enforcement test
  + reviewer + red-team(data-loss) + codex lens.

### R4 — Remaining real debt + comment hygiene (partial-parallel; final merge after R0-R3 settle names)
- D-F1 reconciliation default-ON (after R0 routing). D-STUBS(b) interactive install (implement or document
  json-only). **B5** edge-proposal expiry (wire TTL setter+sweeper OR delete dead enum/column). **B6** eval
  runners (resolve the false-green: implement or delete the dead `alaya-eval` subcommands). **B7** auto-
  extractor ref-hints (bounded producer for supersedes_refs/contradicts_refs). **D-EDGEAUDIT** path-relation
  failure → health_inbox surfacing (required). McpBridge tools.* fallback (delete or document). Delete
  D-STUBS(c) with R0. D-R7 stale comment + ~25 dead phase/deferral comments (I5, task #39). D-BACKLOG:
  rebuild authoritative deferral list AFTER B5/B6/B7 disposed.
- Tasks: #41, #44, #45, #39, + new B5/B6/B7. Gate per sub-item: build/typecheck + targeted Vitest + review-loop.

### Execution-environment note (operator decision 2026-06-03)
The 500q recall-eval OOMs on the local 7.6GB WSL2 box (the bench spins the real daemon + SQLite per
question for fidelity; not an inherent memory-bench cost). So: do ALL code work (R2-R4) + docs (G) +
final 4-lens review (H) HERE with **50q fast-loop** direction validation + per-phase review-loops, then
**MERGE to main** (single source of truth, gate-pending), then switch to a larger machine that just
`git pull`s main and runs the **500q R5 gate** (pure bench execution, zero code change → zero sync
risk). Tag v0.3.11 only AFTER the 500q gate passes; if it fails, fix here → re-merge → re-run. Merging
pre-gate is acceptable pre-launch (version numbers reset; main is not a released artifact).

### R5 — Full 5-bench gate (B1) — runs on the larger machine after merge
ONNX local-model cache setup + embedding-ON preflight. Re-run on HEAD: lme-s 500 OFF, multiturn 500 OFF,
crossquestion 500 OFF, LoCoMo 1982 OFF, LoCoMo 1982 ON — each archive with token-economy + K3/K4 evidence.
KPI eval vs `kpi-targets.md`. (500q OOMs locally → larger host / single heap-capped run; confirm variant/
LoCoMo extraction-cache cost BEFORE any paid run — operator-gated.) Task #14.

### R6 — KPI fail handling — with an AUDITABLE downgrade contract
Holistic root-cause (no constant-tuning). **Do NOT ask the operator to relax 90% until ≥3 fix-loops each
produce the required evidence: same 500q cached archive (or equivalent), R@1/R@5/R@10, the 4 miss-bucket
deltas, budget-dropped best-pre-budget-rank ≤5 vs 6-10 split, path-vs-graph gold-bearing counts,
previously-hit→now-miss displacement, and seed-loss reasons.** Only then AskUserQuestion (90% non-negotiable
vs honest-ship + close condition). Task #7.

### R7 — Docs closeout (G) + final review (H) + merge
Closeout report; README zh/en; runtime-status; backlog (authoritative, post-B5/B6/B7); CHANGELOG;
latest-passing pointers; I4 (label bench synthesis as diagnostic + add production-synthesis-review evidence);
O1 + D-EDGEAUDIT governance-route docs. Final 4-lens review (reviewer + codex + spec-compliance + red-team)
of `main..v0.3.11-completion` → fix-loop to zero → merge (fast-forward) → tag `v0.3.11` → remove worktree.
Tasks #8, #9.

---

## 6. Verification (phase-local gates + end-to-end)
**Phase-local (before dependent phases proceed):** each R0-R4 (and sub-slice) passes build/typecheck +
targeted Vitest (`--maxWorkers=2`/singleFork for OOM) + a doc/source-consistency sweep + a fresh review-loop
to zero Blocking/Important. Dependent workers stop while shared-contract work is not green (handbook
agent-workflow R-rules).
**End-to-end (closeout):** full `rtk pnpm build` clean; full Vitest zero failures; 5 Tier-1 benches pass on
HEAD with token-economy + K3/K4; R@5 proof (top-5 gold movement + path-vs-graph gold-bearing + displacement
+ rank buckets + 5-class cohort attribution); no-network K4.5 passes + host_worker default + no Alaya cloud
call by default; memory dormancy + tombstone-GC fire autonomously with compress/disposition-before-delete
proven (test: no tombstone before compression/disposition); 3+ reviewer lenses zero Blocking/Important.

## 7. Open small decisions (non-blocking; defaults stated)
1. B6 eval runners: default = COMPLETE/resolve (operator stance = no false-green), not exclude.
2. B7 ref-hints: default = complete the bounded producer (no-deferral), not push to v0.4.
3. R5: confirm variant/LoCoMo-ON extraction-cache is cache-shared (free) vs needs paid top-up fill BEFORE any paid run.

## 8. Findings + codex-review index
findings: recall-aggregation-three-layer-rootcause / r2-fanin-design / r2-fanin-carrier-recommendation /
lme-s-500-r5-miss-decomposition / v0.3.11-recall-90-push-report / debt-sweep-docs-and-structural /
debt-sweep-code / garden-sse-streaming-rootcause.
codex: v0.3.11-recall-90-rootcause-review / v0.3.11-recall-90-completeness-review / v0.3.11-masterplan-review.

## 9. Commit log (newest first)
- `96bc2db` fix(recall): lower session_cohort_fanin weight 6→1 (R2 heuristic — pivoting to Route 乙)
- `93feca8` feat(recall): session_cohort_fanin stream (R2 heuristic — pivoting to Route 乙)
- `35915af` docs(core): LLM-verdict recall_allowed band in conflict birth comment (R0.1 B3 fix-loop)
- `e0cf666` fix(v0.3.11): correct B-4 LLM-verdict birth band in truth-boundary docs/tests (R0.1)
- `36b4a5f` docs(v0.3.11): align truth-boundary docs to the two-entry graph model (R0.1 B3)
- `d73dcc2` fix(bench): parse SSE streaming + gate poison shards in garden extractor (pre-effort baseline)
