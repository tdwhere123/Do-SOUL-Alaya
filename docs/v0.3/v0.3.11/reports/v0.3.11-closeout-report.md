# v0.3.11 Closeout Report

**Status:** implementation complete; **KPI gate PENDING the big-machine 500q run.**
**Do NOT read this as "release closed."** v0.3.11 is tagged only after the R5
full-bench gate passes on a larger host (see "KPI gate status" below).

Last updated: 2026-06-04. Branch: `v0.3.11-completion` (worktree). Plan of record:
`.do-it/plans/v0.3.11-completion-masterplan.md` (R0-R7 status + decisions + commit log).

This report maps the codex-surfaced Blockings (B1-B4) and the R-phase work (R0-R4)
to their commits, and states honestly what is **proven** (code + review-loops +
full build + targeted tests) versus **unproven** (the R@5 number, LoCoMo, the
embedding-ON gate).

---

## 1. Proven vs unproven (read this first)

### Proven on this branch
- **Code landed + code-reviewed** for every R0-R4 deliverable below, each through a
  per-phase review-loop (reviewer + codex lens) driven to zero Blocking / zero
  Important before the dependent phase proceeded.
- **Build is clean** and the **targeted Vitest** suites for the touched packages pass
  (`--maxWorkers=2` / single-fork on the 7.6 GB WSL2 box to avoid OOM).
- **Garden compute defaults to zero-cloud** (`host_worker`); the cloud edge-LLM is
  default-OFF; a no-network K4.5 regression asserts no Alaya cloud call by default.
- **The forgetting lifecycle is data-loss-safe.** The autonomous terminal-removal
  path (decay -> dormant -> judged_useless delete) only deletes sourceless,
  never-reinforced rows, and re-verifies at the delete-authority transaction (B1 fix).

### Unproven on this branch (deferred to the R5 big-machine 500q gate)
- **R@5 -> 90% is NOT achieved here.** The recall fan-in (R2) and the faithful
  earned co-recall substrate (R1) are implemented and code-reviewed, but **whether
  R@5 actually reaches 90% is unmeasured locally** and is deferred to the R5 gate on
  a larger machine. The local 7.6 GB WSL2 box OOMs at 500q, and the local 50q
  attempts ran on the wrong (offline-fallback) corpus, so the earlier "fan-in inert"
  local finding is **invalid** — the real-corpus result is genuinely unknown until R5.
- **The five Tier-1 benches are PENDING** that run: LongMemEval-S 500 OFF,
  LongMemEval-S multiturn 500 OFF, LongMemEval-S crossquestion 500 OFF, LoCoMo 1982
  OFF, LoCoMo 1982 local-ONNX ON.
- **LoCoMo (both modes) and the embedding-ON gate are unmeasured** on HEAD.
- **The compress arm of the forgetting lifecycle is DORMANT** pending an operator
  decision (see §4 and backlog `#BL-049`).

**Headline honesty:** R1 + R2 deliver the structural cleanup (heuristic retired,
durable earned-edge carrier, structural-reserve re-aim, path-vs-graph diagnostics).
If the fan-in turns out inert on the real corpus at R5, that cleanup still stands —
but the "R@5 -> 90%" headline does not, and it is **not claimed as achieved anywhere.**

---

## 2. Codex Blockings -> resolution

| Tag | Blocking | Resolution | Commits |
|---|---|---|---|
| B1 | Garden-compute fidelity / zero-cloud (K4.5) not actually default | R0 contract migration: `host_worker` is the product default; cloud edge-LLM strict opt-in; no-network K4.5 regression | `5ceae61`, `3decc57`, `eae265b`, `291e79b` |
| B2 | Bench not production-fidelity for 6b semantic edges | R1: harnesses EARN accepted, recall-eligible positive `recalls` PathRelations through the production co-usage gate (not mere pending proposals) | `05f8012`, `5bc15fb`, `d7aaae1` |
| B3 | Truth-boundary docs drifted from the two-entry graph model | R0.1 doc-align: truth-boundary docs + tests aligned to the two-entry model; LLM-verdict `recall_allowed` band corrected | `36b4a5f`, `e0cf666`, `35915af` |
| B4 | Garden-compute residue (dead providers, cloud default, edge-LLM hardwired) | R0: dead `local_model` / `custom_api` providers removed; B-2 edge classification made a host-worker `EDGE_CLASSIFY` task; zero-cloud default locked | `5ceae61`, `3decc57`, `eae265b`, `291e79b` |

---

## 3. R-phase work -> commits

### R0 — Garden-compute contract migration (B4 + B1 + B3 + D-F1 routing)
Treated as a contract migration, not a flag flip. Dead compute providers removed;
B-2 edge classification deferred out of synchronous enrichment into a claimable
`EDGE_CLASSIFY` host-worker Garden task; the MCP `complete_task` envelope accepts an
edge-verdict result; an explicit eventual-consistency fallback keeps the deterministic
heuristic as the immediate path when recall runs before host-worker classification
completes; `host_worker` is the product default with explicit fallback for
standalone/bench/no-agent; cloud edge-LLM flips to default-OFF behind
`ALAYA_EDGE_PRODUCER_LLM_ENABLED`; a no-network K4.5 regression holds.

Commits: `5ceae61` (remove dead `local_model` + `custom_api` providers),
`3decc57` (B-2 edge classification -> host-worker `EDGE_CLASSIFY` task),
`eae265b` (`host_worker` the zero-own-LLM product default), `291e79b` (lock zero-cloud
default + collapse host-verdict double-mint). Truth-boundary alignment: `36b4a5f`,
`e0cf666`, `35915af`.

### R1 — Bench fidelity (earned co-recall substrate, B2)
Bench harnesses mint same-session co-recall `recalls`-tier edges at seed time and
EARN sparse co-recall paths through the **production** `onCoUsage` gate, so the
archive now contains accepted, recall-eligible positive paths instead of
sub-auto-accept pending proposals.

Commits: `05f8012` (mint same-session co-recall recalls-tier edges at seed time),
`5bc15fb` (EARN sparse co_recalled paths via the production onCoUsage gate),
`d7aaae1` (make graph_expansion hop-2 fan-in test non-vacuous).

### R2 — Durable recall fan-in (the R@5 mechanism, L1/L2/L3 + D-ABSTAIN)
The temporary `session_cohort_fanin` heuristic is retired; durable ACCEPTED positive
`memory_entry <-> memory_entry` co-occurrence hub edges (member -> representative)
are the carrier. Direct hub effects score through `path_expansion`; `graph_expansion`
only covers leftover/multi-hop reach. The structural reserve is gold-blind
relevance-gated and honors suppression; the representative-selection guard is
preserved (nominate ONE query/evidence-relevant representative; refuse
membership-only promotion).

Commits: `9e2d3ad` (retire `session_cohort_fanin`, make durable-edge fan-in the
carrier), `a2b52dd` (gate structural reserve on gold-blind relevance + honor
suppression), `8aa9f05` (type-safe optional fanin access + drop unused binding).

**KPI status:** the R2 code is reviewed here; the **R@5 efficacy (top-5 gold
movement) is DEFERRED to the R5 big-machine 500q gate** — see §1.

### R3 — Compression + forgetting lifecycle (G1 + D-FORGET + D-DECAY-2; incl. the B1 data-loss fix)
- **R3a (G1):** the librarian/auditor synthesis review-proposal `accept` now reaches a
  `synthesis_create` branch that creates a capsule with a deterministic no-LLM summary,
  atomic accept-with-events. This is the memory-compression ENTRY. (`450fa0a`)
- **R3b/R3c:** reversible memory dormancy is wired (dormant demotion + tombstone GC
  enqueued on a timer; previously unwired) and recall/list/FTS exclude DORMANT rows.
  (`bdbabe0`)
- **R3e (D-DECAY-2):** lazy time/idle decay of effective recall strength, computed at
  recall read (bounded, no full-table scan), composed with the competitive timer.
  (`556abeb`)
- **R3d (D-FORGET):** GATED autonomous terminal removal of durable memory — the
  `judged_useless`-delete arm. (`d643be5`)
- **B1 + red-team fixes:** close the data-loss red-team on autonomous deletion — the
  capsule TOCTOU re-verify at the delete-authority transaction; `judged_useless`
  requires evidence == 0 AND reinforcement == 0; non-mergeable negative paths route to
  dormant; stale forget markers clear on non-tombstone transition; dormant -> active
  revival guard; single-count freshness. (`5227380`)
- **Janitor accuracy:** regen semver snapshot + accurate Janitor GC refuse-count.
  (`fb7d4fb`)

**Lifecycle status:** the `decay -> dormant -> judged_useless-delete` arm is LIVE and
B1-data-loss-safe. The **compress arm** (delete consolidated members preserved in a
capsule) is BUILT and safe (delete-time re-verify) but **NOT activated** — it needs
`source_memory_refs` producer wiring + a compress-vs-protection ordering decision +
a lossy-summary-preservation product decision. Tracked as `#BL-049` (`.do-it/` task #64).

### R4 — Remaining real debt + edge/path governance
Edge/path governance debt closed: B5 edge-proposal expiry (TTL setter + `sweepExpired`
sweeper, so `expired` / `expires_at` are a live feature, not dead schema); B7
auto-extractor `contradicts_refs` ref-hint producer; D-EDGEAUDIT path-relation failure
surfaced to `health_inbox`. The seed-materialization batch-loss bug (`candidate_absent`
+ 1963 dropped) is fixed by per-signal failure isolation + a persisted drop reason. The
shipped "not implemented" CLI surfaces are replaced with honest behavior. Stale
phase/history/deferral comments retired from source.

Commits: `dc08442` (edge/path governance debt: B5 expiry + B7 contradicts producer +
D-EDGEAUDIT inbox), `aa2e56c` (isolate seed materialization failures per signal +
persist drop reason), `a2a2f52` (replace shipped "not implemented" surfaces with
honest behavior), `9c738af` (retire stale phase/history/deferral comments).

---

## 4. KPI gate status — PENDING the big-machine 500q

The 500q recall-eval OOMs on the local 7.6 GB WSL2 box (the bench spins the real
daemon + SQLite per question for fidelity). The plan-of-record routes ALL code work
(R0-R4) + docs + final review THROUGH this branch with 50q fast-loop direction
validation + per-phase review-loops, then **merges to main (single source of truth,
gate-pending)**, then a larger host `git pull`s main and runs the **R5 500q gate**
(pure bench execution, zero code change -> zero sync risk).

**v0.3.11 is tagged only AFTER the R5 500q gate passes.** Merging pre-gate is
acceptable pre-launch (version numbers reset; main is not a released artifact). If R5
fails, the holistic root-cause loop (R6) runs HERE — no constant-tuning to bounce a
number, and no asking the operator to relax 90% until >= 3 fix-loops each produce the
required evidence (same 500q cached archive, R@1/R@5/R@10, the 4 miss-bucket deltas,
budget-dropped best-pre-budget-rank split, path-vs-graph gold-bearing counts,
previously-hit -> now-miss displacement, seed-loss reasons).

**R5 gate (from `docs/v0.3/v0.3.11/kpi-targets.md`):**

| Bench | Target | Status on HEAD |
|---|---|---|
| LongMemEval-S 500 embedding OFF | R@5 >= 90% | PENDING |
| LongMemEval-S multiturn 500 embedding OFF | R@5 >= 90% | PENDING (only stale 50q exists) |
| LongMemEval-S crossquestion 500 embedding OFF | R@5 >= 90% | PENDING (only stale 50q exists) |
| LoCoMo 1982 embedding OFF | R@5 >= 55% | PENDING (last measured 44.8% pre-R1/R2) |
| LoCoMo 1982 local ONNX embedding ON | R@5 >= 90% | PENDING (unmeasured; local ONNX cache absent) |

Honest baseline before R1/R2 (500q OFF,
`docs/bench-history/public/2026-06-02T145620Z-d73dcc2-policy-chat`): R@1=52.0%
R@5=81.6% R@10=83.6% (clean, `llm_calls=0`). The 90%@50q earlier number was a
small-sample artifact; clean 90% on the real corpus is **not guaranteed** (~88-89%
floor risk on lme-s).

**Big-machine env recipe** (cache-served, zero paid spend — confirm `llm_calls=0` /
`offline_fallbacks=0` / `path=official_api_compile` before trusting):
`set -a; . ".do-it/bench-env/alaya-api.env"; set +a`. Confirm variant/LoCoMo
extraction-cache cost BEFORE any paid run (operator-gated; the extraction cache covers
only the first ~100 questions, so the full 500q gate needs a one-time paid fill of the
remaining turns).

---

## 5. Deferred items opened as backlog

Genuinely-deferred items surfaced this effort are tracked in
`docs/handbook/backlog.md` with close conditions + due notes:
`#BL-049` (compress-arm activation), `#BL-050` (D-F1 ingest-reconciliation
default-ON), `#BL-051` (#30 abstention re-test on 500q data), `#BL-052`
(LongMemEval CI sample-floor scale-up, was #BL-040), `#BL-053` (edge `llm_supports`
LOCAL pair-classifier), `#BL-054` (lease-pierce governance-cache hot-path hook),
`#BL-055` (Inspector web UI label/filter for `path_relation_failure`). The lowest-ROI
recall-topology refinement `multi_hop_path` stays at `#BL-047`.

---

## 6. Open morning decisions (operator-gated)

1. **Compress-arm activation** (`#BL-049`): wire `source_memory_refs` producer +
   resolve compress-vs-protection ordering + the lossy-summary-preservation product
   call. Until then only the `judged_useless`-delete arm runs.
2. **R5 paid extraction-cache fill** (kpi §3): confirm variant/LoCoMo extraction-cache
   is cache-shared (free) vs needs a paid top-up fill BEFORE any paid run.
3. **R6 honest-ship vs 90%-non-negotiable** (only if R5 fails after >= 3 evidence-backed
   fix-loops): AskUserQuestion with a written close condition.
