# Implementation Brief: v0.3.0-slice-6 — Close-out: backlog, runtime-status, §25 note, version bump, release notes

> - **Card ID**: v0.3.0-slice-6
> - **Source/Background**: `docs/v0.3/README.md`; closes `#BL-009`, `#BL-037`, `#BL-038`; records the cumulative v0.3.0 surface change
> - **Target**: `docs/handbook/backlog.md`, `docs/handbook/runtime-status.md`, `docs/handbook/invariants.md` (§25 worked-example note), all workspace `package.json` carrying `0.2.0`, root `package.json` (if versioned), `docs/v0.3/v0.3.0/release-notes.md` (new), `docs/v0.3/README.md` + `docs/v0.3/v0.3.0/README.md` (card status), `README.md` (status badge), `docs/v0.3/v0.3.0/reports/v0.3.0-closeout.md` (new). (The former `docs/v0.2/v0.2.1/` and `docs/v0.2/v0.2.2/` plan folders were already removed and `docs/v0.2/README.md` already updated when the cards were authored — nothing left to do there.)
> - **Size**: S (docs + version strings)
> - **Prerequisite**: v0.3.0-slice-1, v0.3.0-slice-2, v0.3.0-slice-3, v0.3.0-slice-4, v0.3.0-slice-5
> - **Blocks**: none — wave-closing card; the wave-end review-loop and branch close-out follow
> - **Owner**: unassigned

## 1. Background & Goal

**Background.** Slices 1–5 land the keychain adapter (`#BL-009`) and
the host-autonomy evidence (`#BL-037`, `#BL-038`). Slice 1's schema
change (the `secret_ref` accepted shapes + the `secret_ref_kind`
EventLog enum) is an additive change on two §25-covered surfaces — a
minor bump — and `semver-surface.test.ts`'s snapshot has already
moved. The handbook, the release notes, and the workspace version
must now reflect the v0.3.0 state. (The former `docs/v0.2/v0.2.1/`
and `docs/v0.2/v0.2.2/` plan folders were removed and
`docs/v0.2/README.md` updated when these cards were authored.)

**Goal.** Mark `#BL-009` / `#BL-037` / `#BL-038` Resolved, update
`runtime-status.md` (secret_ref readiness + `MCP tool surface` →
`agent-used`), add the §25 worked-example note, bump `0.2.0 → 0.3.0`
across the workspace, write the v0.3.0 release notes (stating the
§25 minor rationale), update the README status badge and the v0.3
card-status tables.

## 2. Allowed Scope

### docs/handbook/backlog.md

- **Change**: move `#BL-009`, `#BL-037`, `#BL-038` to the "Resolved
  (short closure summaries)" section with v0.3.0 evidence:
  - `#BL-009` — keychain for secrets. Resolved in v0.3.0.
    `resolveSecretRef` gains a `keychain:<service>:<account>` branch
    (`apps/core-daemon/src/secrets.ts` + `secrets/keychain/{macos,linux,windows}.ts`);
    `alaya doctor` verifies a configured `keychain:` ref; `alaya
    install --keychain` migrates an existing ref. Evidence:
    v0.3.0-slice-1 .. slice-3 + the per-platform manual transcripts
    under `docs/v0.3/v0.3.0/reports/`.
  - `#BL-038` — host autonomous use of `soul.*`. Resolved in v0.3.0
    (or: carried to `#BL-NNN` if slice 4's fallback was taken — then
    leave `#BL-038` open pointing at the new issue and say so here).
    Evidence: the `<host>-<version>` fixture + `host-autonomy-replay.test.ts`.
  - `#BL-037` — Codex `/alaya-inspect` slash recognition. Resolved
    in v0.3.0 — positive (documented format + dispatch transcript)
    or negative (`cli-consumable` retired, `maintenance.md` version
    note). Evidence: v0.3.0-slice-5.
- **Change**: confirm the "Next available number" header is correct
  (`#BL-039` unless slice 4's fallback opened one — then it advances
  by one and the new issue is listed under Open Issues / Deferred
  with `#BL-038`'s acceptance list as its close condition).

### docs/handbook/runtime-status.md

- **Change**: update the `secret_ref` / P4-secrets readiness row —
  `keychain:` supported on macOS / Linux / Windows, with the
  caveat "Linux path CI-exercisable when `secret-tool` present;
  macOS and Windows ship recorded manual transcripts".
- **Change**: move the `MCP tool surface` readiness from
  `mcp-callable` to `agent-used` for the validated host(s), citing
  the v0.3.0-slice-4 fixture + replay test (or, if slice 4's
  fallback was taken, leave `mcp-callable` and note the carried
  `#BL-NNN`).
- **Change**: apply the Codex slash-recognition line from
  v0.3.0-slice-5's outcome (kept + version, or `cli-consumable`
  removed) — whichever side the wave README assigned the edit to;
  do not double-edit.

### docs/handbook/invariants.md

- **Change**: append a worked-example note to §25 (do **not**
  renumber §25): v0.3.0's minor was driven by adding `keychain:` to
  the `secret_ref` accepted shapes (`packages/protocol/src/app-config.ts`)
  and `keychain` to the `secret_ref_kind` EventLog enum
  (`packages/protocol/src/events/garden.ts`) — the textbook §25
  "new accepted value / new enum value that is not a discriminator
  key" minor case; `semver-surface.test.ts`'s snapshot moved and was
  updated in v0.3.0-slice-1.

### Workspace package.json files

- **Change**: bump `"version": "0.2.0"` → `"0.3.0"` in every
  workspace `package.json` (`apps/core-daemon`, `packages/protocol`,
  and every other workspace package; root `package.json` too if it
  carries a version). Sweep with `grep -rl '"version": "0.2.0"'
  --include=package.json`. Sibling consumers pinned `^0.2.0` re-pin
  `^0.3.0`.

### docs/v0.3/v0.3.0/release-notes.md (new)

- **Change**: write the v0.3.0 release notes — (a) the keychain
  feature (the three platform adapters, `alaya doctor` check, `alaya
  install --keychain` migration, the reversibility property); (b) an
  explicit SemVer statement: "This is a **minor** bump per invariant
  §25 because it adds `keychain:` to the accepted `secret_ref`
  shapes (`packages/protocol/src/app-config.ts`) and `keychain` to
  the `secret_ref_kind` EventLog enum (`packages/protocol/src/events/garden.ts`);
  sibling consumers pinned `^0.2.0` should re-pin `^0.3.0`"; (c) the
  host-autonomy evidence (`#BL-038`'s fixture + replay regression,
  or the carried `#BL-NNN`) and the `#BL-037` outcome; (d) the
  locations of the macOS / Windows (and Linux, if not CI) manual
  transcripts under `docs/v0.3/v0.3.0/reports/`.

### docs/v0.3/README.md + docs/v0.3/v0.3.0/README.md

- **Change**: fill in the v0.3 codename if one is chosen; flip the
  card-status table to final status; record the slice 4 fallback
  outcome if it was taken.

### README.md

- **Change**: bump the status badge from `status-v0.1.3` to
  `status-v0.3.0`; optionally add a one-line note in the roadmap
  section that v0.3.0 closed the keychain gap (`#BL-009`) and the
  host-autonomy / Codex-slash evidence items (`#BL-037` / `#BL-038`).
  Do not weaken any honest-gap language.

### docs/v0.2/ (already done — verify only)

- **No change expected.** The `docs/v0.2/v0.2.1/` and
  `docs/v0.2/v0.2.2/` plan folders were removed and
  `docs/v0.2/README.md` (cadence table, layout tree,
  cross-references) was updated when these cards were authored.
  Slice 6 only re-checks that nothing reintroduced a dangling
  `v0.2.1/` / `v0.2.2/` reference (`grep -rn 'docs/v0.2/v0.2.[12]'`
  returns nothing).

### docs/v0.3/v0.3.0/reports/v0.3.0-closeout.md (new)

- **Change**: wave close-out report — reviewed by, closed at commit,
  scope (`#BL-009` / `#BL-037` / `#BL-038` and any carried issue),
  build/test evidence (the full-suite gate), the §25 SemVer
  declaration, intentional deviations, the slice 4 fallback decision
  if taken, follow-up readiness impact.

## 3. Deferred

- The v0.3 codename, if not chosen during this slice — left unset;
  not a backlog item (purely cosmetic).
- If slice 4's fallback was taken, the real Codex/Claude host
  recording is carried by the new `#BL-NNN` opened in slice 4 — this
  card records that, it does not re-open a second issue.
- Nothing else deferred.

## 4. Acceptance Criteria

| AC | Criteria | Evidence |
|---|---|---|
| AC1 | `#BL-009` / `#BL-037` / `#BL-038` are Resolved in `backlog.md` with v0.3.0 evidence (or `#BL-038` points at the carried `#BL-NNN` if the fallback was taken); the "Next available number" header is correct. | `grep -A 4 'BL-009' docs/handbook/backlog.md`; `grep -A 4 'BL-037' …`; `grep -A 4 'BL-038' …`; the header |
| AC2 | `runtime-status.md` reflects keychain on three platforms (Linux=CI / macOS+Windows=manual transcript) and the `MCP tool surface` readiness move (or the carried-issue note), and the Codex slash-recognition line matches slice 5's outcome. | `grep -n 'keychain\|MCP tool surface\|alaya-inspect' docs/handbook/runtime-status.md` |
| AC3 | `invariants.md` §25 has the v0.3.0 worked-example note and §25's number is unchanged. | `grep -A 3 'v0.3.0' docs/handbook/invariants.md`; §25 still numbered 25 |
| AC4 | `docs/v0.3/v0.3.0/release-notes.md` exists and (a) summarizes the keychain feature, (b) states the §25 minor rationale + the `^0.2.0` → `^0.3.0` re-pin, (c) covers the host-autonomy / `#BL-037` outcomes, (d) lists the manual-transcript locations. | `cat docs/v0.3/v0.3.0/release-notes.md` |
| AC5 | Every workspace `package.json` is `0.3.0`. | `grep -rn '"version": "0.2.0"' --include=package.json` returns zero hits |
| AC6 | The README status badge is `status-v0.3.0`. | `grep -n 'status-v0' README.md` |
| AC7 | Full build, full test suite, and `tsc -b` are green (wave-end gate). | `rtk pnpm build`; `rtk pnpm test`; `rtk pnpm exec tsc -b` — all green |
| AC8 | `docs/v0.3/v0.3.0/README.md` lists all six cards with their final status; no dangling `docs/v0.2/v0.2.1/` or `docs/v0.2/v0.2.2/` reference remains. | the file; `grep -rn 'docs/v0.2/v0.2.[12]'` returns nothing |

## 5. Verification

```bash
grep -A 4 'BL-009' docs/handbook/backlog.md
grep -A 4 'BL-037' docs/handbook/backlog.md
grep -A 4 'BL-038' docs/handbook/backlog.md
grep -n 'keychain' docs/handbook/runtime-status.md
grep -n 'status-v0' README.md
grep -rn '"version": "0.2.0"' --include=package.json   # must be empty
grep -rn 'docs/v0.2/v0.2.[12]'                        # must be empty
ls docs/v0.3/v0.3.0/release-notes.md
rtk pnpm exec tsc -b
rtk pnpm test
rtk pnpm build
```

The `grep -rn '"version": "0.2.0"' --include=package.json` MUST
return zero hits.

## 6. Shared File Hazards & Dependencies

- `docs/handbook/backlog.md`, `runtime-status.md`, `invariants.md`,
  and every workspace `package.json` are this card's exclusive
  territory at the close-out level — slices 1–5 do not touch them
  there (slice 5 may have pre-applied its single `runtime-status.md`
  line by the wave README's assignment; honour that). Land slice 6
  last so the readiness summary and the version reflect the final
  state of the wave.
- If a wave-end review-loop finding requires a code fix after slice
  6, the fix lands as its own atomic commit; slice 6's docs are
  re-touched only if the fix changes a public surface (then §25's
  "declare the SemVer step" applies to the fix commit too).

**Prerequisite**: v0.3.0-slice-1 .. v0.3.0-slice-5.
**Blocks**: none — wave-closing card; the wave-end review-loop and
branch close-out (`do-it-branch-closeout` + `do-it-verification-gate`)
follow.
