# v0.3.0 Closeout Wave — Review (consolidated)

Scope reviewed: `git diff 335d0a0..bd26626` — the host-worker config entry
(`ALAYA_GARDEN_PROVIDER_KIND` / `install` `garden_provider_kind`,
`patchRuntimeGardenComputeEnvFile` mirror, `defaultRuntimeGardenComputeConfig`
read), the `ALAYA_AGENT_TARGET` attach stamp, the `#BL-038` live-usage
witness (`scripts/export-host-autonomy-witness.mjs`,
`host-autonomy-witness.test.ts`, the `claude-code-live` fixture), the
keychain platform-review notes, the workspace version bump, and the
README / runtime-status doc updates.

Lenses: Claude `reviewer` (PR-style correctness) + Codex independent
second-model review (via `codex exec`). Two rounds.

## Round 1 — findings

| # | Severity | Where | Finding | Resolution |
|---|----------|-------|---------|------------|
| B1 | Blocking (Codex) / Nice-to-have (Claude — "consistent with existing repo practice, not a regression") | `host-autonomy-fixtures/claude-code-live/metadata.json` + `export-host-autonomy-witness.mjs` | `source_db` wrote the operator's absolute home path (`/home/<user>/...`) into the committed fixture. Severity reconciled to "fix it". | `redactDbPath` collapses a home-relative path to `~/...` and otherwise keeps only the file name; fixture regenerated. (Round-2 follow-up: also split on `\` so a Windows-style path on a POSIX host can't leak the directory.) |
| I1 | Important (Codex) | `export-host-autonomy-witness.mjs` | `hostLabel` CLI arg interpolated into `path.join` — `../` could write outside the fixtures dir. | `hostLabel` validated against `^[a-z0-9][a-z0-9-]*$`; resolved fixture path confined under the fixtures root or the script `fail()`s. |
| I2 | Important (Codex) | `profile-mutation.ts` | `ALAYA_AGENT_TARGET` only written at attach time; `detectAttachedProfileInstructionsDrift` only compared `operator_instructions`, so `alaya doctor` reported a pre-stamp profile as `in_sync` while its telemetry stayed in the `mcp` bucket. | Drift detection now also requires the `ALAYA_AGENT_TARGET` stamp to match (`extractAttachedAgentTarget` + the existing `absent`/`drifted`/`in_sync` outcomes preserved); regression tests for both hosts added. |
| I3 | Important (Codex) / residual-risk note (Claude — "intended design: env is the bootstrap, SQLite is the live truth") | `install.ts`, README, runtime-status | `garden_provider_kind` in `install --non-interactive` only writes `.env`; once the Inspector has written a persisted `runtime:garden-compute` row, that row wins and the env value is inert. | Kept the design (consistent with every other env-derived default); README + runtime-status reworded to spell out the precedence; added a `routes-config-port.test.ts` case pinning "persisted row wins over a disagreeing env default". |
| N1 | Nice-to-have (both) | `export-host-autonomy-witness.mjs` | DB open/query errors threw a raw `better-sqlite3` stack instead of the script's friendly-error style. | DB open + query wrapped; failures routed through the script's `fail(...)` path. |
| N2 | Nice-to-have (Claude) | `export-host-autonomy-witness.mjs` | better-sqlite3 resolver hardcoded the `@12.9.0` pnpm path; the bare-`require` fallback also fails from `scripts/`. | Resolves `better-sqlite3` version-agnostically by scanning `node_modules/.pnpm/better-sqlite3@*` after the bare `require`. |
| N3 | Nice-to-have (Claude) | `config-service.ts` + `install.ts` | The `RuntimeGardenComputeConfigSchema.unwrap().shape.provider_kind` expression was re-derived in two places. | `RuntimeGardenProviderKindSchema` exported from `@do-soul/alaya-protocol` (named to avoid collision with the unrelated compute-routing `GardenProviderKind` enum) and imported by both. |
| N4 | Nice-to-have (Claude) | metadata.json | (Same artifact as B1.) | Covered by the B1 fix. |

Claude reviewer's verdict on round 1: **accept** (zero Blocking, zero
Important) with the four nice-to-haves. Codex's round-1 list raised B1 to
Blocking and I1–I3 to Important; severities reconciled above and all
addressed in the `fix(v0.3.0): address review-loop findings` commit.

Confirmed-OK in round 1 (both lenses): the `enabled` semantics change in
`defaultRuntimeGardenComputeConfig` (no regression on the default path —
`enabled === (providerKind === "official_api" && secretRef !== null)`
reduces to the old `secretRef !== null` when no kind is declared; an
explicit non-`official_api` kind correctly yields `enabled: false`); the
slice-3 B1 install narrowing is not re-violated (new code touches only the
`.env`, never the SQLite governance row, and errors rather than silently
drops a bad value); the committed `event-log.jsonl` contains only
hashes / ids / `agent_target` / counts / timestamps — no recalled content,
no free text; `host-autonomy-witness.test.ts` pins the chain shape
(`pointer_count >= 1`, linked `delivery_id`, host-typed `agent_target`,
usage not preceding delivery).

## Round 2 — re-review of the fix-loop commit

Codex re-reviewed `git diff bd26626..83d7fb3`:

- **Blocking**: none.
- **Important**: one — `redactDbPath`'s `path.basename` fallback is
  separator-platform-based, so a Windows-style path string passed on a
  POSIX/WSL host (`C:\Users\<user>\...`) would still write the directory
  (and username) into `metadata.source_db`.
- Confirmed I1 / I2 / I3 / N1 fixed; B1 fixed for the committed fixture
  but the script redaction was incomplete (the Important above).

Resolution: `redactDbPath` now splits on both `/` and `\` for the
file-name fallback. A re-re-review of that 3-line change in a maintenance
script's redaction helper was judged disproportionate (Standard tier,
no build/test impact); the script's behavior is exercised by the round-2
manual checks (export still emits `~/.config/alaya/alaya.db`; a
Windows-style arg errors gracefully and would have redacted to `alaya.db`).

## Outcome

Zero Blocking / zero Important outstanding. Build + `tsc -b` clean;
296 test files / 2428 tests passing. Cleared for merge.
