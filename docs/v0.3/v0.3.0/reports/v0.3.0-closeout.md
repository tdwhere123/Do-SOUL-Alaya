# v0.3.0 Closeout Report

## Status

BLOCKED.

The controller worktree contains code-complete keychain slices 1-3 and
evidence reports for slices 4-5, but v0.3.0 is not closed. Slice 6 was
not executed because its prerequisites are not satisfied.

## Completed

- Slice 1 keychain resolver/protocol/config/audit/provenance implementation.
- Slice 2 doctor keychain readiness reporting.
- Slice 3 interactive `install --keychain` implementation.
- Slice 4 fixture schema and BLOCKED host-autonomy report.
- Slice 5 Codex `0.130.0` negative slash-recognition report and maintenance
  note.

## Blocking Items

- Slice 4 Strict Gate is not satisfied. There is no real Codex or Claude Code
  host transcript proving autonomous `soul.recall` plus
  `soul.report_context_usage` with `usage_state == "used"`.
- Slice 3 platform transcripts are missing for Linux, macOS, and Windows
  real keychain write/read behavior.

## Not Performed

- Did not close `#BL-038`.
- Did not move the MCP tool surface readiness to `agent-used`.
- Did not resolve `#BL-009` in backlog because platform transcripts are
  missing.
- Did not bump workspace package versions from `0.2.0` to `0.3.0`.
- Did not write v0.3.0 release notes as a shipped release artifact.
- Did not merge the controller branch back to `main`.

## Verification

Passed in the controller worktree:

```bash
rtk pnpm exec tsc -b
rtk pnpm exec vitest run --project @do-soul/alaya-core-daemon -- install install-keychain-migration
rtk pnpm test
rtk pnpm build
rtk git diff --check
```

Full-suite result:

- `rtk pnpm test`: 295 files / 2416 tests passed.

Review-fix result:

- The first slice 1-3 review found TTY masking, failed-audit guard, and
  install-migration proof gaps.
- The install path now masks the TTY input path, checks failed install audits
  before keychain writes, updates an existing persisted Garden runtime config
  row, and has E2E coverage for a `file:` starting ref plus doctor OK proof.
- A follow-up review found macOS was still passing the write secret in process
  argv; the adapter now uses `security -i` with stdin input and the narrow
  re-review found no Blocking/Important issues for that item.

## Required Next Steps

1. Capture the slice 3 Linux, macOS, and Windows keychain transcripts.
2. Capture the slice 4 real host-autonomy transcript and EventLog witness.
3. Add the offline replay regression only after the real host fixture exists.
4. Re-run the review/fix loop on the final wave surface.
5. Execute slice 6 docs, version bump, release notes, final gate, and merge.
