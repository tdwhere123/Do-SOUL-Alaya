# v0.3.0 Slice 2 Report - Doctor Keychain Readiness

## Status

CODE COMPLETE in the controller worktree.

This slice only covers doctor/status reporting for keychain-backed Garden
compute credentials. It does not close the platform transcript or host-autonomy
gates.

## Work Completed

- Extended `GardenComputeStatus.credential_source` with `keychain`.
- Added `GardenKeychainCheck` to report success, missing entry, or missing
  platform tooling for keychain-backed Garden compute.
- Wired doctor JSON and human output so keychain failures degrade the Garden
  check and produce operator-readable remediation without printing secret
  values.
- Kept non-keychain Garden compute status unchanged; env/file refs do not emit
  a `keychain_check` field.

## Verification

Passed:

```bash
rtk pnpm exec tsc -b
rtk pnpm exec vitest run --project @do-soul/alaya-core-daemon -- doctor
rtk git diff --check
```

Notes:

- The doctor filtered run overmatched the project and passed 67 files / 506
  tests when this slice was gated.

## Residual Risk

Doctor readiness is mocked at the keychain reader boundary. It proves reporting
and remediation behavior, not the host OS keychain tools themselves.
