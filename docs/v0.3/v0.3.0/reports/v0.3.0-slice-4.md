# v0.3.0 Slice 4 Report - Host Autonomy Replay

## Status

BLOCKED.

The Strict Gate is not satisfied. I did not create a synthetic host transcript,
did not create `apps/core-daemon/src/__tests__/host-autonomy-replay.test.ts`,
and do not claim `#BL-038` closed.

## Evidence Checked

- `docs/v0.3/v0.3.0/v0.3.0-slice-4-host-autonomy-replay.md` requires a real
  Codex or Claude Code session that autonomously calls `soul.recall` and
  `soul.report_context_usage`, with offline replay only after a real fixture
  exists.
- `docs/handbook/backlog.md #BL-038` requires a real MCP stdio session, host
  invocation within normal conversation, a non-empty recall delivery, and a
  matching usage report with `usage_status` / `usage_state` equal to `used`.
- `docs/handbook/runtime-status.md` still classifies the MCP Agent-Use Protocol
  as `mcp-callable`, with `agent-used` deferred to `#BL-038`.
- Local host CLIs are available:
  - Codex: `codex-cli 0.130.0`
  - Claude Code: `2.1.139`
- Default local Alaya config directory resolved to `/home/tdwhere/.config/alaya`;
  it contains `alaya.toml` and `alaya.db`.
- Local EventLog query against `/home/tdwhere/.config/alaya/alaya.db` found:
  - `soul.recall.delivered`: 57 rows
  - `soul.context_usage.reported`: 26 rows
  - usage states: `not_applicable` = 17, `skipped` = 9, `used` = 0

## Blocking Finding

No local EventLog witness currently proves the required host-autonomy chain.
The latest telemetry rows are MCP-originated and include recall deliveries, but
there is no `soul.context_usage.reported` row with `usage_state == "used"`.
There is also no preserved MCP stdio transcript, attach-written
`operator_instructions` snapshot, fresh install/attach metadata, or fixture
state tying a real host conversation to those rows.

## Work Completed

- Added `docs/v0.3/v0.3.0/host-autonomy-fixtures/README.md` to pin the fixture
  schema and minimum proof requirements without fabricating a recording.
- Recorded this BLOCKED report so slice 6 and parent orchestration can avoid
  overclaiming `agent-used` or `#BL-038` closure.

## Required Parent Action

Capture a real host session before replay implementation:

1. Fresh install in an isolated config directory.
2. Fresh `alaya attach codex` or `alaya attach claude`.
3. Preserve the attach-written operator instructions.
4. Run a recall-worthy prompt in the real host conversation.
5. Confirm EventLog has matching `soul.recall.delivered` and
   `soul.context_usage.reported` rows for the same delivery, with
   `usage_state == "used"` and non-empty recall pointers.
6. Save transcript, metadata, EventLog rows, and seed state under
   `docs/v0.3/v0.3.0/host-autonomy-fixtures/<host>-<version>/`.

Only after that should `host-autonomy-replay.test.ts` be added.
