/** Shared Garden post-turn excerpt cap. Protocol is the leaf home for this bound. */
export const POST_TURN_EXTRACT_EXCERPT_MAX_CHARS = 800;

/** Logical enrichment contract identity for per-source Garden work. */
export const SOURCE_ENRICHMENT_CONTRACT = "source_enrichment.v1";
