// Inert: always false. Family decorrelation replaced this lever; the export
// stays so env maps that still list ALAYA_RECALL_LEXICAL_DECORR remain stable.
export function lexicalDecorrEnabled(): boolean {
  return false;
}
