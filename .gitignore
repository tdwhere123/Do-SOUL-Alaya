node_modules/
.pnpm-store/
dist/
build/
coverage/
*.tsbuildinfo
.turbo/
.next/
.cache/
.claude/
.vscode/

# Secrets / local env (.env.example stays tracked)
.env
.env.local
.env.*.local

# Local runtime data
var/
data/
apps/bench-runner/data/
# Regenerated bench extraction cache (hash-keyed; not source truth)
docs/bench-history/datasets/longmemeval-extraction-cache/
# Regenerated edge-auto-producer decision cache (content-keyed; not source truth)
docs/bench-history/datasets/edge-auto-producer-decisions/
# Regenerated reconciliation-decision cache (model-keyed; not source truth).
# The upstream extraction cache is already ignored, so committing this layer
# buys no zero-LLM reproducibility on a fresh checkout — only per-model bloat.
docs/bench-history/datasets/reconciliation-decisions/
# External full bench diagnostics; tracked archives keep compact sidecars only.
.bench-artifacts/
# Per-run diagnostic detail; tracked run archives keep compact sidecars only
# (kpi.json / report.md / findings.md / longmemeval-cold-warm-comparison.json).
docs/bench-history/**/longmemeval-diagnostics.json
docs/bench-history/**/locomo-diagnostics.json
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo

# Tooling local config
.worktrees/

# Note: .codex and .claude are KEPT in git per user instruction
# Note: vendor/do-what-new-snapshot/ was the v0.1 port-time source
#       reference; removed by Phase E vendor cleanup after v0.1.0.
#       See CLAUDE.md §Project Genealogy for the upstream commit hash.

# do-it plugin internal artifacts (per-worktree plan stub for grill hook)
.do-it/
.gitnexus
