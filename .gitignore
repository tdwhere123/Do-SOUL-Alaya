node_modules/
.pnpm-store/
dist/
build/
coverage/
*.tsbuildinfo
.turbo/
.next/
.cache/
.claude/

# Local runtime data
var/
data/
apps/bench-runner/data/
# Regenerated bench extraction cache (hash-keyed; not source truth)
docs/bench-history/datasets/longmemeval-extraction-cache/
# External full bench diagnostics; tracked archives keep compact sidecars only.
.bench-artifacts/
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo

# Tooling local config
.worktrees/

# Note: .codex and .claude are KEPT in git per user instruction
# Note: vendor/do-what-new-snapshot/ was the v0.1 port-time source
#       reference; removed by Phase E vendor cleanup after v0.1.0.
#       See CLAUDE.md §Project Genealogy for the upstream commit hash.

# do-it plugin internal artifacts (per-worktree plan stub for grill hook)
.do-it/
