{
  "name": "soul-memory-product-local",
  "private": true,
  "type": "module",
  "version": "0.0.0-local",
  "description": "Local-only SOUL Memory product prototype.",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "soul-memory": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.5"
  }
}
